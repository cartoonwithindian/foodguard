# Vercel
.vercel

# Node
node_modules/
.next/
out/
dist/
*.tsbuildinfo
.next/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts / archives
*.zip
*.rar
*.tar.gz
*.7z

# Local env files (keep secrets out of git)
.env
.env.local
.env.*.local

# Bundled models / binaries (committed via LFS or kept local)
models/
certificates/

# Local scratch files
txt.txt
venv_packages.txt
project.tar.gz
foodguard-fssai-api.zip

# Legacy calibration / comparison artifacts
legacy-*.md
legacy-*.csv
legacy-*.json
recommendation-audit-results.json

# Machine-generated search index artifacts (kept out of git)
*.onnx
*.npy
*.bin
*.download.jpeg

# Nested git repositories (each has its own remote; not added as gitlinks here)
/backend/
/barcode-benchmark/
